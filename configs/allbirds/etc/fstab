# /etc/fstab: static file system information.
#
# Use 'blkid' to print the universally unique identifier for a
# device; this may be used with UUID= as a more robust way to name devices
# that works even if disks are added and removed. See fstab(5).
#
# <file system> <mount point>   <type>  <options>       <dump>  <pass>
/dev/mapper/allbirds_raid1-allbirds_root /               ext4    errors=remount-ro 0       1
/dev/mapper/allbirds_raid1-allbirds_home /home           ext4    defaults        0       2
/dev/mapper/allbirds_raid1-allbirds_tmp /tmp            ext4    defaults        0       2
/dev/mapper/allbirds_raid1-allbirds_usr /usr            ext4    defaults        0       2
/dev/mapper/allbirds_raid1-allbirds_var /var            ext4    defaults        0       2
nodev       /dev/hugepages  hugetlbfs pagesize=1GB 0 0 
